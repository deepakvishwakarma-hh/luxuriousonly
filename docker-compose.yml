services:
  postgres:
    image: postgres:15-alpine
    container_name: luxuriousonly-postgres
    environment:
      POSTGRES_USER: medusa
      POSTGRES_PASSWORD: medusa_password
      POSTGRES_DB: medusa_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medusa"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - luxuriousonly-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: luxuriousonly-backend
    environment:
      NODE_ENV: production
      MEDUSA_ADMIN_ONBOARDING_TYPE: default
      DATABASE_URL: postgresql://postgres:AhFcwyPhEVBlAcVvJRGctLzCJEnCciSA@turntable.proxy.rlwy.net:41329/railway
      STORE_CORS: http://localhost:8000,http://frontend:8000,https://admin.luxuriousonly.com,https://luxuriousonly.com
      ADMIN_CORS: http://localhost:7001,http://localhost:9000,https://admin.luxuriousonly.com
      AUTH_CORS: http://localhost:7001,http://localhost:9000,http://localhost:8000,http://frontend:8000,https://admin.luxuriousonly.com,https://luxuriousonly.com
      JWT_SECRET: e3a7f1d9b4c2a9e6f8c0d1a4b7c3e9f2a6b8d0c1e4f7a9b3c5d6e2f1a8
      COOKIE_SECRET: e3a7f1d9b4c2a9e6f8c0d1a4b7c3e9f2a6b8d0c1e4f7a9b3c5d6e2f1a8
      VITE_BACKEND_URL: https://admin.luxuriousonly.com
      BACKEND_URL: https://admin.luxuriousonly.com
      SMTP_HOST: smtp.zeptomail.in
      SMTP_PORT: 587
      SMTP_USER: emailapikey
      SMTP_PASS: PHtE6r1bE+nigzEv9RUAsfbqR87yNIos9e1lfQdOt44TXKIGGk1S+NEvkTXhqB55XPVHEKGSwIxqubuUu+7UcW+4YWsaXWqyqK3sx/VYSPOZsbq6x00asV8Zf0zeVIXmcdFr0CTXuN7eNA==
      SMTP_FROM: fullstackartists.com
      SMTP_SECURE: false
    ports:
      - "9000:9000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - backend_uploads:/app/static
    networks:
      - luxuriousonly-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://localhost:9000/health || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY: pk_e71f37f08cb8091285da83451d131a5ed93f496262d3e2a76f66381f5c7256cb
        NEXT_PUBLIC_MEDUSA_BACKEND_URL: https://admin.luxuriousonly.com
        NEXT_PUBLIC_BASE_URL: https://luxuriousonly.com
    container_name: luxuriousonly-frontend
    environment:
      NODE_ENV: production
      # Server-side backend URL (for internal Docker network)
      MEDUSA_BACKEND_URL: https://admin.luxuriousonly.com
      # Client-side backend URL (for browser access)
      NEXT_PUBLIC_MEDUSA_BACKEND_URL: https://admin.luxuriousonly.com
      NEXT_PUBLIC_BASE_URL: https://luxuriousonly.com
      NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY: pk_e71f37f08cb8091285da83451d131a5ed93f496262d3e2a76f66381f5c7256cb
    ports:
      - "8000:8000"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - luxuriousonly-network
    restart: unless-stopped

volumes:
  postgres_data:
  backend_uploads:

networks:
  luxuriousonly-network:
    driver: bridge
