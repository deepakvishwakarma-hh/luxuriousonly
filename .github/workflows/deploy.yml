name: Deploy Backend & Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p $HOME/.ssh
          ssh-keyscan -H 31.97.226.111 >> $HOME/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 31.97.226.111
          username: root
          key: |
            -----BEGIN OPENSSH PRIVATE KEY-----
            
            -----END OPENSSH PRIVATE KEY-----
          port: 22
          debug: true
          script: |

            set -e  # Exit immediately if a command exits with a non-zero status

            echo "🚀 Deploying BACKEND"
            cd /root/luxuriousonly/backend || { echo "❌ Backend directory not found!"; exit 1; }

            # Configure git to avoid prompts
            git config --global --add safe.directory /root/luxuriousonly/backend || true
            git config pull.rebase false || true

            echo "📥 Pulling latest changes..."
            timeout 60 git pull origin main || { echo "❌ Git pull failed or timed out"; exit 1; }

            echo "📦 Installing dependencies..."
            npm install --force

            # Rebuild native dependencies for correct architecture
            echo "🔧 Rebuilding native dependencies..."
            npm rebuild @swc/core || true

            echo "🔨 Building..."
            npm run build

            echo "✅ Backend code updated"

            echo "🚀 Deploying FRONTEND"
            cd /root/luxuriousonly/frontend || { echo "❌ Frontend directory not found!"; exit 1; }

            # Configure git to avoid prompts
            git config --global --add safe.directory /root/luxuriousonly/frontend || true
            git config pull.rebase false || true

            echo "📥 Pulling latest changes..."
            timeout 60 git pull origin main || { echo "❌ Git pull failed or timed out"; exit 1; }

            echo "📦 Installing dependencies..."
            npm install --force

            echo "🔨 Building..."
            npm run build

            echo "✅ Frontend code updated"

            echo "🔄 Reloading PM2 from project root..."
            cd /root/luxuriousonly || { echo "❌ Project root not found!"; exit 1; }

            if [ -f ecosystem.config.js ]; then
              echo "📋 Found ecosystem.config.js, reloading PM2 apps..."
              pm2 reload ecosystem.config.js || pm2 start ecosystem.config.js || { echo "❌ PM2 reload/start failed"; exit 1; }
            else
              echo "❌ ecosystem.config.js not found!"
              exit 1
            fi

            echo "✅ Deployment completed"

            echo ""
            echo "📊 Deployment Summary:"
            pm2 list
            echo ""
            echo "✅ CI/CD Pipeline completed successfully!"
